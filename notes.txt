FIRST: get rid of all data for rooms that dont have custom names (make sure to keep the data for rooms that currently have custom names). add a few more custom names for rooms that dont have them yet (make them actually funny and short and add some that contain profanity) but dont add too many more, some rooms should still not havve data yet

THEN:
MAKE THE FOLLOWING CHANGES FOR x page, ONLY MAKE NECESSARY CODE EDITS AND TRY TO STICK TO ORIGINAL CODE INTENT...

the 'house rooms' page (contains the house emblem at the top and all of the rooms for that house in rows):
"
Custom names should be asterixed out by default if they contain profanity (only the parts of the name that contain profanity should be asterixed). Add a visiblity toggle switch that reveals them (just make it a simple icon of an eye being crossed out or smthg similar next to the toggle, place the toggle in such a way that no more vertical space for it is taken up by solely the toggle itself, but still ensure that it is easily accessible and visible and not cluttering things) <-- see the censorship toggle logic that is currently implemented for the 'all rooms' tab and make it follow a format more similar to the house rooms page. then modify the current 'all rooms' page so that it appears more similar to the house rooms page (decide whatever is best to do for the elements that are currently house specific so that whatever is there makes sense since the all rooms page contains data from all houses, not just one specific one. perhaps some quick stats that a user would actually want to see (think about what would make the most sense to see if you were searching for a room to pick across all houses on campus))

make the custom names appear in maroon and keep the default ones the same color

italicize the default name (and its parantheses) that appears under the custom name

without changing the total space taken up by the hero section at the top with the emblem, make the dorm name appear below the house name and make the font just a tiny bit smaller

get rid of the red underline underneath the house name

respace or slightly lower the font size of all the elements in the white transparent section underneath the hero so that it all fits in one line (keep the font sizes as big as possible without making things too big)

add another preview bubble to the hero on the top right that doesnt cycle between text but is instead in maroon and displays static text that says smthg along the lines of 'click to access house board and clicking it takes you to the house board'
"

the 'house board' page (displays full trivia, insider tips, and house chatter ):
"
add left/right arrows to the navigation buttons inside house trivia and insider tips

move the house trivia section start closer to the left of the page, at the same position that the table starts on the 'house rooms' page (do the mirror opposite for the insider tips section so that it ends where the table ends at the house rooms page)

make the house chatter section feel less bounded, remove the boundaries or make them transparent (see whatever looks better ui wise)

make it detect which preview window you selected to access the board page and briefly highlight the corresponding section on the board page once it loads (so the user gets visual feedback based on which preview bubble they clicked and it doesnt just feel like all the sections take you to the same page)

remove the community notes coming soon banner at the bottom

animate the quotes a tiny bit when you hover over them (decide whatever is the coolest way to give visual feedback)

get rid of the house chatter label entirely and just directly display the quotes below the house trivia and insider tips
"